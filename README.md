# ML_GAME_FACE_CONTROL
This is a ml project in python which performs certain actions using handtracker and pypunut
pip install opencv-python
pip install cvzone
pip install mediapipe
pip install pynput.
